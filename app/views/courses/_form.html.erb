<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <br>

  <div class="field course_form">
    <%= form.label :name, class: "sr-only" %>
    <%= form.text_field :name, placeholder: "Nombre", class: "form-control" ,required: true %>
  </div>

  <div class="field course_form">
    <%= form.label :course_code, class: "sr-only"%>
    <%= form.text_field :course_code, placeholder: "Codigo Del Curso", class: "form-control" %>
  </div>



  <div class="field

    <%= form.label :start_date %>
    <%= form.date_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.date_select :end_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
