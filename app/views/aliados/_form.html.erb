<%= form_with(model: aliado, local: true) do |form| %>
  <% if aliado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aliado.errors.count, "error") %> prohibited this aliado from being saved:</h2>

      <ul>
      <% aliado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :tipo_socio %>
    <%= form.select :tipo_socio, ['Linea', 'Presencial'] , class:"form-control"%>
  </div>

  <div class="field">
    <%= form.label :correo %>
    <%= form.text_field :correo %>
  </div>

  <div class="field">
    <%= form.label :contacto %>
    <%= form.text_field :contacto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
