wb = xlsx_package.workbook
wb.add_worksheet(name: "Buttons") do |sheet|

  sheet.add_row ["Usuario", "Contraseña", "Nombre", "Paterno", "Materno"]

  @students.each do |student|
    sheet.add_row [student.username, student.internal_password, student.name, student.first_last_name, student.second_last_name]
  end
end
