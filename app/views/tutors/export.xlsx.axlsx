wb = xlsx_package.workbook
wb.add_worksheet(name: "Buttons") do |sheet|

  sheet.add_row ["Usuario", "Contraseña", "Nombre", "Paterno", "Materno"]

  @tutors.each do |tutor|
    sheet.add_row [tutor.username, tutor.internal_password, tutor.name, tutor.first_last_name, tutor.second_last_name]
  end
end
